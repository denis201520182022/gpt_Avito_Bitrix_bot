# 🤖 GPT-бот для Авито и Bitrix24

Чат-бот для обработки сообщений с Авито и отправки ответов в Bitrix24. Использует FastAPI, OpenAI GPT и Redis для хранения истории диалогов. Работает напрямую через Python и виртуальное окружение, без Docker.

## 🏗️ Архитектура

- **FastAPI** - веб-сервер для обработки вебхуков
- **OpenAI GPT** - генерация ответов на сообщения
- **Redis** - хранение истории диалогов и очереди сообщений
- **Асинхронные воркеры** - обработка сообщений в фоне для каждого диалога
- **Ротация логов** - автоматическое управление размером лог-файлов

## 📂 Структура проекта

```
gpt_bot_Avito_Bitrix/
├── bot.py                    # Основной код бота
├── config.py                 # Конфигурация через .env.prod
├── .env.prod                 # Файл с переменными окружения
├── requirements.txt          # Зависимости Python
├── prompt.txt                # Файл с системным промптом для GPT
├── logs/
│   └── bot.log              # Логи работы бота
└── README.md                # Этот файл
```

## 🚀 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd gpt_bot_Avito_Bitrix
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Установка Redis

```bash
sudo apt update
sudo apt install redis-server -y
sudo systemctl enable redis
sudo systemctl start redis

# Проверка работы Redis
redis-cli ping   # должно ответить PONG
```

### 5. Настройка переменных окружения

Создайте файл `.env.prod` с следующими переменными:

```bash
# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here
GPT_MODEL=gpt-3.5-turbo

# Bitrix24
BITRIX_WEBHOOK=https://your-portal.bitrix24.ru/rest/your_user_id/your_webhook_key/
BOT_ID=your_bot_id
CLIENT_ID=your_client_id

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Промпт (путь к файлу)
PROMPT_FILE=


## ▶️ Запуск бота

### Локальный запуск для тестирования

```bash
source venv/bin/activate
uvicorn bot:app --host 0.0.0.0 --port 8000 --reload
```

### Продакшн запуск

```bash
source venv/bin/activate
uvicorn bot:app --host 0.0.0.0 --port 8000 --workers 2
```



## 🔧 Настройка Bitrix24 и Авито

### Настройка Bitrix24

1. Создайте чат-бота в Bitrix24
2. Получите `BOT_ID` и `CLIENT_ID` 
3. Настройте вебхук для получения сообщений, указав URL: `http://your-server:8000/bot`

### Настройка Авито

1. В личном кабинете Авито подключите интеграцию с Bitrix24
2. Убедитесь, что сообщения с Авито приходят в ваш Bitrix24

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Последние логи
tail -f logs/bot.log

```

### Проверка статуса Redis

```bash
redis-cli info replication
redis-cli dbsize  # количество ключей в базе
```

### Мониторинг системного сервиса

```bash
sudo systemctl status gpt-bot
sudo journalctl -u gpt-bot -f  # следить за логами сервиса
```

## 🛠️ Настройки и параметры

### Основные параметры в коде

- `MAX_HISTORY_PAIRS = 50` - максимальное количество пар сообщений в истории
- `WORKER_TIMEOUT = 300` - таймаут воркера в секундах (5 минут)
- `OPENAI_RETRIES = 3` - количество попыток при ошибке OpenAI
- Размер лог-файла: 5MB с ротацией (3 бэкапа)


## 🔍 Troubleshooting

### Бот не отвечает

```bash
# Проверьте статус сервиса
sudo systemctl status gpt-bot

# Проверьте логи
tail -f logs/bot.log

# Проверьте Redis
redis-cli ping
```

### Ошибки OpenAI

- Проверьте корректность API ключа
- Убедитесь, что на аккаунте есть кредиты
- Проверьте лимиты rate limit

### Проблемы с Bitrix24

- Проверьте корректность вебхука
- Убедитесь, что бот активен в Bitrix24
- Проверьте права доступа бота

### Очистка Redis (при необходимости)

```bash
redis-cli flushdb  # очистить текущую базу
redis-cli flushall # очистить все базы
```

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `tail -f logs/bot.log`
2. Проверьте статус сервисов: `sudo systemctl status gpt-bot redis-server`
3. Проверьте конфигурацию в `.env.prod`
4. Убедитесь в доступности внешних API (OpenAI, Bitrix24)

---
